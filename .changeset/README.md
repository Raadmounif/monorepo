# Changesets

Drop a markdown file in this folder to describe a change to one or more shared packages. Generate one with `pnpm changeset` and follow the prompts.

Apps (`@raadmounif/web`, `@raadmounif/api`) are ignored — they're deployed, not published.
